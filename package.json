{
  "name": "abhishek-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio site (static HTML + compiled Tailwind CSS).",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
